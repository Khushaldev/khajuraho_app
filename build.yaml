targets:
  $default:
    builders:
      json_serializable:
        options:
          include_if_null: false
          explicit_to_json: true
        generate_for:
          - lib/dto**/*.dart
          - lib/models**/*.dart
          - lib/core/app_config**/*.dart